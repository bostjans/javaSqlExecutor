sudo: false

language: java

jdk:
  - openjdk8

cache:
  directories:
    - "$HOME/.cache"

before_install:
  - cp .travis.settings.xml ~/.m2/settings.xml
  - curl -i http://developer.dev404.net/maven/

deploy:
  provider: script
  script: "curl -T \"README.md\" --user developertest@dev404.net:public_html7 ftp://developer.dev404.net/"
  skip_cleanup: true

after_script:
  - ./copy2media.sh
